
<ul class="work-feature">
    <li>
        <div class="work-item">
            <img src="/images/teds-mob-1.png" alt="Ted's Cameras product page">
        </div>
        <div class="work-hover">
            <span>Ted's Cameras <div>2021</div></span>
            <!--<img src="/images/teds-prod-desktop.jpg" alt="Ted's Cameras product page">-->
        </div>
    </li>
    <li>
        <div class="work-item">
            <img src="/images/highgrove-mob-1.png" alt="Ted's Cameras product page">
        </div>
        <div class="work-hover">
            <span>Highgrove Bathrooms <div>2020</div></span>
            <!--<img src="/images/hg-prod-desktop.jpg" alt="Ted's Cameras product page">-->
        </div>
    </li>
    <li>
        <div class="work-item">
            <img src="/images/pedders-mob-1.png" alt="Ted's Cameras product page">
        </div>
        <div class="work-hover">
            <span>Pedders <div>2023</div></span>
            <!--<img src="/images/pedders-prod-desktop.jpg" alt="Ted's Cameras product page">-->
        </div>
    </li>
</ul>

<style lang="scss">
    
/*
    .work-feature {
        position: relative;
        list-style: none;
        margin: auto;
        height: 80dvh;
        max-height: 900px;
          flex-grow: 1;
        padding-left: 3rem;
        grid-gap: 2rem;
        flex-grow: 1;

        li {

            &:nth-child(1) img {
                top: 30%;
                right: 0;
                z-index: 1;

            }

            &:nth-child(2) img {
                top: 50%;
                right: clamp(8.5vw, 100px, 14vw);
                z-index: 2;
            }

            &:nth-child(3) img {
                top: 70%;
                right: clamp(17vw, 200px, 28vw);
                z-index: 3;
            }

            &:hover {

                .work-hover {
                    opacity: 1;

                    img {
                        opacity: 1;
                        transform: translateY(0);
                        scale: 1;
                    }

                    span {
                        transform: translateY(0);
                        opacity: 1;
                    }
                }
            }
        }

        .work-item {
            max-width: 300px;
            aspect-ratio: 207 / 305;
            transform: translateY(-50%);
            position: absolute;
            width: 300px;
            object-fit: cover;
              object-position: top;
              border-radius: 10px;
            box-shadow: 3px 3px 6px rgba(0,0,0,.125), 1px 1px 18px rgba(0,0,0,.1);
            transition: all 0.2s;
            position: absolute;
            z-index: 5;
            text-align: center;
            inset: auto;
            margin: auto;
        }

        .work-hover {
            display: flex;
            height: 100%;
            width: 100%;
            object-fit: cover;
            position: fixed;
            z-index: 4;
            inset: 0;
            margin-inline: auto;
            background-color: rgb(255 200 132 / 30%);
            align-items: center;
            justify-content: center;
            padding: 3rem;
            pointer-events: none;
            opacity: 0;
            backdrop-filter: blur(2px) grayscale(0.5);
            transition: opacity 0.5s;

            img {
                max-width: 85vw;
                max-height: 85dvh;
                box-shadow: 3px 3px 20px rgba(0,0,0,.1), 6px 6px 20px rgba(0,0,0,0.05);
                box-shadow: none;
                border-radius: 10px;
                transform: translateY(15px);
                scale: 0.99;
                opacity: 0;
                transition: transform 0.6s, scale 0.6s, opacity 0.5s;
            }

            span {
                font-size: 4.5rem;
                line-height: 1;
                text-transform: uppercase;
                font-weight: 400;
                font-family: 'Cormorant SC', serif;
                margin-top: 10rem;
                margin-bottom: 1rem;
                padding: 4rem 8rem;
                color: #111;
                background: radial-gradient(ellipse at center,rgba(255,200,132,.7) 0,rgba(255,200,132,.55) 50%,rgba(255,200,132,.25) 60%,rgba(255,200,132,0) 75%,rgba(255,200,132,0) 100%);
                text-shadow: 0 0 16px hsla(0,0%,100%,.85),2px 2px 12px hsla(0,0%,100%,.5),2px 2px 22px rgba(255,221,179,.8),2px 2px 30px rgba(255,231,201,.9),0 0 10px hsla(0,0%,100%,.65);
                text-transform: uppercase;
                position: absolute;
                z-index: 5;
                text-align: center;
                inset: auto;
                margin: auto;
                transform: translateY(-20px);
                opacity: 0;
                transition: all 0.5s;
            }
        }
    }
*/

.work-feature {
        list-style-type: '';
        position: relative;
        width: 600px;
        height: 600px;
        margin: auto;

        .work-item {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            mask-size: contain;
            mask-position: center;
            mask-repeat: no-repeat;

            img {

            }
        }

        li:nth-child(1) {

            .work-item {
                mask-image: url('/images/pie.svg');
            }

            img {
                position: absolute;
                top: 0;
                right: 0;
                object-position: top;
                width: 51%;
                height: 85%;
                object-fit: contain;
                object-position: top;
            }
        }
        
        li:nth-child(2) {

            .work-item {
                mask-image: url('/images/pie2.svg');
            }

            img {
                height: 51%;
                object-fit: cover;
                object-position: center;
                width: 89%;
                position: absolute;
                bottom: 0;
                left: 6%;
            }
        }
        
        li:nth-child(3) {

            .work-item {
                mask-image: url('/images/pie3.svg');
            }

            img {
                width: 51%;
                height: 85%;
                object-fit: contain;
                object-position: top;
            }
        }

        .work-hover {
            display: flex;
            height: 100%;
            width: 100%;
            object-fit: cover;
            position: fixed;
            z-index: 4;
            inset: 0;
            margin-inline: auto;
            background-color: rgb(255 200 132 / 30%);
            align-items: center;
            justify-content: center;
            padding: 3rem;
            pointer-events: none;
            opacity: 0;
            backdrop-filter: blur(2px) grayscale(0.5);
            transition: opacity 0.5s;

            img {
                max-width: 85vw;
                max-height: 85dvh;
                box-shadow: 3px 3px 20px rgba(0,0,0,.1), 6px 6px 20px rgba(0,0,0,0.05);
                box-shadow: none;
                border-radius: 10px;
                transform: translateY(15px);
                scale: 0.99;
                opacity: 0;
                transition: transform 0.6s, scale 0.6s, opacity 0.5s;
            }

            span {
                font-size: 4.5rem;
                line-height: 1;
                text-transform: uppercase;
                font-weight: 400;
                font-family: 'Cormorant SC', serif;
                margin-top: 10rem;
                margin-bottom: 1rem;
                padding: 4rem 8rem;
                color: #111;
                background: radial-gradient(ellipse at center,rgba(255,200,132,.7) 0,rgba(255,200,132,.55) 50%,rgba(255,200,132,.25) 60%,rgba(255,200,132,0) 75%,rgba(255,200,132,0) 100%);
                text-shadow: 0 0 16px hsla(0,0%,100%,.85),2px 2px 12px hsla(0,0%,100%,.5),2px 2px 22px rgba(255,221,179,.8),2px 2px 30px rgba(255,231,201,.9),0 0 10px hsla(0,0%,100%,.65);
                text-transform: uppercase;
                position: absolute;
                z-index: 5;
                text-align: center;
                inset: auto;
                margin: auto;
                transform: translateY(-20px);
                opacity: 0;
                transition: all 0.5s;
            }
        }
    }
</style>